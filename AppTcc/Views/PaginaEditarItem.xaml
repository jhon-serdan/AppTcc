<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppTcc.Views.PaginaEditarItem"
             Title="PaginaEditarItem"
             NavigationPage.HasBackButton="False"
             Shell.NavBarIsVisible="False"
             NavigationPage.HasNavigationBar="False"
             Shell.TabBarIsVisible="False">

    <VerticalStackLayout>

        <Label 
            Text="Editr Receita"
            Style="{StaticResource TituloPagina}"/>

        <Label Text="Valor Receita" 
               Style="{StaticResource TituloItens}"/>

        <Entry x:Name="EntryValorReceitaPagEdt"
               Style="{StaticResource DefaultStyleEntryNmrc}" />

        <Label 
            Text="Data"
            Style="{StaticResource TituloItens}"/>

        <DatePicker x:Name="DtpckReceitaPagEdt"
                    Style="{StaticResource DefaultStyleDtpck}"/>

        <Label 
            Text="Categoria"
            Style="{StaticResource TituloItens}"/>

        <Picker x:Name="PckCategoriaReceitaPagEdt"
                Style="{StaticResource DefaultStylePck}"
                SelectedIndex="0">

            <Picker.Items>
                <x:String>-- Selecione --</x:String>
            </Picker.Items>

        </Picker>

        <Label Text="Descrição"
               Style="{StaticResource TituloItens}" />

        <Entry x:Name="DescricaoReceitaPagEdt"
               Placeholder="Informe a descrição"
               Style="{StaticResource DefaultStyleEntryAbcd}" />


    </VerticalStackLayout>


</ContentPage>